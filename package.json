{
  "name": "sr-nestjs-utils",
  "version": "0.0.1",
  "description": "libreria de utilidades para nestjs",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "nest build && cp package.json dist/",
    "prepublish": "npm run build"
  },
  "author": "sorodriguezz",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^10.3.3",
    "@nestjs/core": "^10.3.3",
    "reflect-metadata": "^0.2.1",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.27",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.2"
  }
}
